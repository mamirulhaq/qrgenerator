<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>QR Code Generator</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #fefefe;
            display: flex;
            flex-direction: column;
            align-items: center;
            padding-top: 40px;
        }
        h1 {
            color: #333;
        }
        form {
            margin-bottom: 20px;
        }
        input[type="text"] {
            padding: 8px;
            width: 300px;
            font-size: 16px;
        }
        button {
            padding: 8px 16px;
            font-size: 16px;
            margin-left: 10px;
        }
        img {
            margin: 20px 0;
            border: 1px solid #ccc;
        }
        .download-btn {
            display: inline-block;
            padding: 8px 16px;
            background-color: #007bff;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
        .download-btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>QR Code Generator</h1>
    <form action="/" method="POST">
        <input type="text" name="data" placeholder="Masukkan teks..." required>
        <button type="submit">Generate</button>
    </form>

    {% if qr_base64 %}
        <div style="text-align: center;"style="text-align: center;">
            <h3>This is your QR Code</h3>
            <img src="data:image/png;base64,{{ qr_base64 }}" alt="QR Code" width="200">
            <br>
            <a href="data:image/png;base64,{{ qr_base64 }}" download="qrcode.png" class="download-btn">Download QR</a>
        </div>
    {% endif %}
</body>
</html>
